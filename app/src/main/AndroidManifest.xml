<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.arvth.letsgetfed">

    <application
        android:allowBackup="true"
        android:icon="@drawable/apps_for_good_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".Controller">

        <activity android:name=".FoodManager"></activity>

        <activity android:name=".Pantry">
        </activity>

        <!--<activity android:name=".Shelf">-->
        <!--</activity>-->

        <activity android:name=".AddFood"></activity>

        <activity android:name=".ShelfManager"></activity>

        <activity android:name=".Alert"></activity>

        <activity android:name=".Counter"></activity>

        <activity android:name=".Fridge"></activity>

        <activity android:name=".Freezer"></activity>

        <activity android:name=".Settings"></activity>

    </application>

</manifest>